Сюда можно положить портреты Людмилы. Затем пропишите пути в `config.js`:

- `photos.envelope` — фон конверта
- `photos.hero` — первое фото после открытия
- `photos.plates` — кадры между блоками
- `photos.close` — фото в конце письма
